# СмартФикс — Учёт заявок на ремонт

## 📌 Описание проекта

Подсистема **«СмартФикс. Учёт заявок на ремонт»** предназначена для сервисных организаций, занимающихся ремонтом цифровой техники. Система автоматизирует процесс обработки заявок, упрощает взаимодействие между сотрудниками и ускоряет обслуживание клиентов.

Приложение предоставляет удобный веб‑интерфейс для мастеров, клиентов и администратора, позволяя управлять заказами, отслеживать статусы и формировать отчёты.

---

## 🎯 Основные возможности

### Для неавторизованных пользователей:

* просмотр списка услуг
* регистрация в системе.

### Для клиента:

* создание новых заявок на ремонт
* просмотр всех своих заявок
* отслеживание статусов ремонта
* редактирование контактной информации.

### Для мастера:

* просмотр заявок, назначенных на него
* изменение статусов ремонта
* добавление комментариев к заявке

### Для администратора:

* полный доступ ко всем заявкам
* назначение мастеров на заявки
* добавление и удаление услуг
* управление пользователями (регистрация, блокировка и др.)

### Дополнительный функционал:

* фильтрация заявок по категориям
* просмотр детальной информации о заказах
* генерация отчётов в формате **PDF** по текущим и выполненным заявкам.

---

## 🛠️ Технологический стек

### Backend

* **C#**, **ASP.NET Core MVC** — основа серверной части.
* **MySQL 8.0+** — система управления базами данных.
* **Docker**, **docker-compose** — контейнеризация и удобное развёртывание.
* **Caddy** — веб‑сервер с автонастройкой HTTPS.

### Frontend

* **React** — построение пользовательского интерфейса.

### Средства разработки

* **Visual Studio** — основная IDE, поддержка Docker, ASP.NET Core и MySQL.
* **MySQL Workbench** — администрирование и управление базой данных.

---

## 📂 Архитектура

Приложение реализовано на основе MVC‑паттерна:

* **Models** — описание сущностей (заявки, пользователи, услуги).
* **Views** — интерфейс клиента на React.
* **Controllers** — логика обработки запросов.
* **API** — взаимодействие фронтенда и бекенда.

---

## ⚙️ Эксплуатационные требования

### Серверная сторона:

* ОС: Ubuntu 18.04+
* MySQL 8.0.40+
* CPU: 2 ядра × 2 ГГц
* RAM: от 2 ГБ
* ПО: Docker, docker-compose, MySQL Workbench

### Клиентская сторона:

* ОС: Windows 10+
* CPU: от 1 ГГц
* RAM: от 1 ГБ
* Свободное место: 40 МБ
* Постоянный доступ к интернету

### 1. Клонирование репозитория

```bash
git clone https://github.com/username/smartfix.git
cd smartfix
```

### 2. Запуск через Docker Compose

```bash
docker-compose up -d --build
```

### 3. Приложение будет доступно по адресу:

```
http://localhost:80
```

---

## 📁 Структура проекта

```
smartfix/
├── backend/            # ASP.NET Core сервер
├── frontend/           # React интерфейс
├── docker-compose.yml  # Конфигурация контейнеров
├── caddy/              # Настройки веб-сервера
└── README.md           # Документация
```

---

## 📄 Генерация PDF‑отчётов

Система поддерживает создание отчётов:

* по текущим заявкам;
* по завершённым заказам.

Отчёты автоматически формируются сервером и доступны к скачиванию.

---
